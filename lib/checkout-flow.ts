/**
 * @deprecated [KHÔNG KHUYÊN DÙNG] 
 * Hãy sử dụng `@/application/use-cases/checkout/checkout-flow.use-case` để thay thế.
 * 
 * File này chỉ đóng vai trò là cửa ngõ tương thích ngược (Compatibility stub).
 * Luồng thanh toán (Checkout flow) đã được chuyển sang Lớp Ứng dụng (Application Layer).
 */
export * from "@/application/use-cases/checkout/checkout-flow.use-case";

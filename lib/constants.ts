/**
 * Các hằng số bổ trợ cho Thư viện (Library Constants)
 */

/** Trạng thái môi trường thực thi (development, production, test) */
export const NODE_ENV = process.env['NODE_ENV'] || "development";

/**
 * Tầng Ứng dụng (Application Layer)
 * 
 * Nơi chứa đựng các logic nghiệp vụ cụ thể (Use Cases).
 * Các Components giao diện sẽ gọi trực tiếp vào tầng này thay vì gọi xuống Repository.
 */
export * from "./use-cases";

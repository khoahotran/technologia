/**
 * Wishlist API placeholder.
 * Currently the wishlist is stored client-side only.
 * Swap these stubs with real API calls when the backend adds wishlist endpoints.
 */

export async function syncWishlist(_productIds: string[]): Promise<void> {
    // TODO: POST /api/wishlist/sync when backend ready
}

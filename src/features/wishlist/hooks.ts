/**
 * Wishlist feature hooks — thin wrappers over the Zustand store.
 * No server state needed until the backend exposes wishlist endpoints.
 */

export { useWishlist, useWishlistStore } from '@/store/wishlist.store';
